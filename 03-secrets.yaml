---
# PNETLab Secrets
# Credenciais sensíveis (ALTERE ANTES DE USAR EM PRODUÇÃO!)
apiVersion: v1
kind: Secret
metadata:
  name: pnetlab-secrets
  namespace: pnetlab
  labels:
    app.kubernetes.io/name: pnetlab
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Credenciais padrão do PNETLab (ALTERE!)
  ADMIN_USERNAME: "admin"
  ADMIN_PASSWORD: "pnet"  # MUDE ISSO!
  
  # Credenciais do MySQL/MariaDB interno
  MYSQL_ROOT_PASSWORD: "pnetlab_root_2024"  # MUDE ISSO!
  MYSQL_DATABASE: "pnetlab"
  MYSQL_USER: "pnetlab"
  MYSQL_PASSWORD: "pnetlab_db_2024"  # MUDE ISSO!
  
  # Chave de API (se aplicável)
  API_KEY: ""
